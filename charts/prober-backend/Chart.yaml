apiVersion: v2
name: prober-backend
description: Central service for the prober looking glass — agent gRPC gateway, web UI (REST + SSE), and Prometheus metrics.
type: application
# Placeholders; the release workflow sets both to the git tag via
# `helm package --version <tag> --app-version <tag>`.
version: 0.0.0
appVersion: "0.0.0"
kubeVersion: ">= 1.25.0-0"
home: https://github.com/didww/prober
sources:
  - https://github.com/didww/prober
maintainers:
  - name: DIDWW Engineering
    email: engineering@didww.com
keywords: [traceroute, mtr, looking-glass, network, prober]
