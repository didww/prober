# prober-agent Helm values. One release per site.

replicaCount: 1

image:
  repository: ghcr.io/didww/prober-agent
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""
serviceAccountName: ""

# This agent's site id and how it reaches the backend.
site: ""                        # required, e.g. "fra"
backend:
  address: ""                   # required, e.g. "prober-backend.example.com:50051"
  token: ""                     # required unless existingSecret is set
  # The backend's TLS trust. Give the CA inline, or leave empty to use the
  # image's system roots (for a publicly-trusted backend certificate).
  ca: ""
  serverName: ""
  # Verify the chain to the CA but not the hostname/SAN — useful when the agent
  # dials the backend by a Kubernetes Service name that is not in the cert.
  skipHostnameVerify: false
  insecureSkipVerify: false

limits:
  maxConcurrentJobs: 20
  maxCycles: 1000
  minIntervalMs: 100
  maxTtl: 40

# Where the DNS tool sends its questions. Empty uses the pod's resolv.conf,
# which with hostNetwork and ClusterFirstWithHostNet is the node's resolver:
# the point of the tool is to show what the site's own resolver answers. Set
# it only when that resolv.conf is not the one that matters. Entries are an
# address or address:port.
dns:
  nameservers: []
  #  - 10.0.0.53
  #  - "[2001:db8::53]:53"

# Provide the whole config.yml via an existing Secret (key config.yml) instead
# of the values above — useful when the token comes from an external secret store.
existingSecret: ""

# The agent's probes must leave from the node's real interface, not the pod
# overlay, or the first hop and source address are wrong.
hostNetwork: true

podAnnotations: {}
podLabels: {}
podSecurityContext:
  seccompProfile:
    type: RuntimeDefault
# Raw sockets need CAP_NET_RAW. The distroless image runs as root (uid 0) with
# every other capability dropped, which is what lets an added capability be
# effective without file capabilities.
securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  runAsNonRoot: false
  runAsUser: 0
  capabilities:
    drop: [ALL]
    add: [NET_RAW]

resources:
  requests:
    cpu: 25m
    memory: 32Mi
  limits:
    memory: 128Mi

nodeSelector: {}
affinity: {}
tolerations: []
